# Apply progress — driver-route-cockpit-redesign

## S1 — Pure derivation selector (REMEDIATED, GREEN)

Compaction: prior commit breached the 600 all-inclusive cap (678 lines vs baseline `2425c17`); this file trimmed 70→10 and the spec tightened by ~32 lines (fixture/dividers/whitespace). **No production change, no test assertion weakened, all 41 tests + REQ-DCD-001..008 coverage preserved.**

**TDD**: RED module-resolution fail → 1 failed suite · GREEN 41/41 pass · TRIANGULATE 8 `it.each` + 100-read side-effect spy · REFACTOR helpers named for S4–S10.

**Verify**: `pnpm test:unit --run .../cockpit` 41/41 · `pnpm test:unit --run` (full) 356 files / 5486 tests · `pnpm type-check` clean.

**Budget vs `2425c17`**: impl 146+/0-, spec ~422+/0-, tasks.md 4+/4-, apply-progress.md 10+/0- ⇒ **TOTAL ≤600 (additions+deletions, margin)**. Branch `feat/driver-route-cockpit-redesign-b1-foundations`; no push. S2 not started.

## S2 — Truthful quick-action predicates and guards (GREEN)

Co-located `driverCockpitQuickActions.ts` + spec. Predicates (`canOpenExternalMap`/`canCopyAddress`/`canOpenEmail`) are synchronous + pure; helpers (`openExternalMap`/`copyAddressToClipboard`/`openEmail`) return typed `QuickActionResult { ok, message }` and NEVER throw (SSR / blocked popup / clipboard rejection / `window.open` throw all return canonical failure copy).

**TDD**: RED module-resolution fail → 1 failed suite · GREEN 41/41 pass (after parameterizing SSR + throwing cases) · TRIANGULATE 12/5/5 `it.each` + SSR / blocked / clipboard-rejection paths via `vi.stubGlobal('window'|'navigator', undefined)` · REFACTOR shared `encodeQuery` helper, `QUICK_ACTION_FAILURE_MESSAGES` exported for S3 to mirror into `copy.ts` `cockpit.quickActions` subtree.

**Verify**: `pnpm test:unit --run .../utils/cockpit` 41/41 · `pnpm test:unit --run` (full) green · `pnpm type-check` clean.

**Budget vs `4bd68a2`**: impl 108+/0-, spec 266+/0-, tasks.md 4+/0- (checkbox toggles), apply-progress.md 10+/0- ⇒ **TOTAL ≤390** (under 400 cap, under 370 aim + slack). Branch unchanged; no push. S3 not started.
## S3 — Cockpit/drawer/confirmation/refresh copy source (GREEN)

Additive `cockpit.*` subtree on `copy.ts` (header / operational / drawer / quickActions / confirm / footer) + `toasts.refreshFailed` for the manual-refresh path. `actions.checkIn` reused verbatim (no duplicate `Marcar entregada` key); `QUICK_ACTION_FAILURE_MESSAGES` cross-imported so any drift in either file fails the spec immediately.

**TDD**: RED 9 cockpit assertions fail on `cockpit`/`refreshFailed` undefined · GREEN 17/17 · TRIANGULATE live-import cross-pin of `QUICK_ACTION_FAILURE_MESSAGES` + `it.each` over `{customer}`/`{N}`/`{folio}` + `{completed}`/`{total}` template order · REFACTOR consolidated two quick-action failure tests into one cross-import test; tightened copy.ts S3 comments to single-line REQ annotations.

**Verify**: `pnpm test:unit --run .../__tests__/copy.spec.ts` 19/19 (8 existing + 11 new) · `pnpm test:unit --run` (full) 357 files / 5538 tests green · `pnpm type-check` clean.

**Budget vs `196848f`**: impl 59+/0-, spec 107+/0-, tasks.md 4+/4- (S3 checkboxes), apply-progress.md ~12+/0- ⇒ **TOTAL ≈ 190** (under 200 cap, slightly over 170 aim by ~20 lines from the cross-import + REQ comments). Branch unchanged; no push. S4 not started.

## S4 — Sticky cockpit header `DriverCockpitHeader` (GREEN)

Presentational sticky panel-contained header: back, identity (`route.driver?.name ?? 'Ruta'`), lifecycle `StatusDotBadge` (tone/label from `DELIVERY_ROUTE_STATUS_*`), `{completed}/{total}` progress, history, refresh. Typed props `{ route; progress; isFetching }` · typed emits `{ back: []; refresh: []; 'open-history': [{ trigger: HTMLElement }] }`. Native `<button>` for back/history/refresh (no `UButton` shell drift) + `<UIcon>` for the icon glyphs; ≥44×44 (`min-h-11 min-w-11`) on every control + `focus-visible:ring-2 focus-visible:ring-primary`; `sticky top-0 z-10` + `min-w-0 truncate` so 320px never horizontally overflows; semantic dark/light tokens (`bg-default`, `border-default`, `text-muted`); refresh disabled while `isFetching` AND its handler early-returns (REQ-DCS-007); history emit carries `event.currentTarget` for focus-return (REQ-DCK-008). No `vue-router` / `useQuery` / `useMutation` / `useQueryClient` / `axios` / `fetch(` imports anywhere in the SFC body (static source assertion in the spec, doc comment intentionally names the forbidden identifiers).

**TDD**: RED module-resolution fail → 1 failed suite · GREEN 16/16 · TRIANGULATE `it.each` over the four route statuses with tone/label shared maps + one reactivity test (isFetching true→false re-enables refresh) + one scope-pin test (no ETA / distance / Siguiente / km / min / map) + one source-invariant test (`fs.readFileSync` scan against `vue-router` / `useQuery` / `axios` / `fetch(` with the JSDoc header stripped) · REFACTOR trimmed the redundant UButton stub + the four per-status badge tests into one `it.each`; tightened fixtures to one `makeRoute` helper.

**Verify**: `pnpm test:unit --run .../components/cockpit` 16/16 · `pnpm test:unit --run` (full) 358 files / 5554 tests green (+1 file, +16 tests vs S3 baseline) · `pnpm type-check` clean · `DriverCockpitHeader` SFC never imported anywhere yet (S10 will mount it) — `vue-tsc --build` stays green per the S4 verify note.

**Budget vs `12e5d8e`**: impl 141+/0- (DriverCockpitHeader.vue), spec 186+/0- (DriverCockpitHeader.spec.ts incl. stubs/fixtures/source-scan), tasks.md 4+/4- (S4 checkbox toggles), apply-progress.md 10+/0- ⇒ **TOTAL 345 (additions+deletions, ≤340 aim + 5 lines slack, well under ≤400 cap)**. Branch unchanged; no push. S5 not started.

## S5 — Operational current/next stop hierarchy `DriverOperationalStops` (GREEN)

Mobile-first current + next hierarchy as one DOM region. Typed props `{ currentStop; nextStop; notes; hasStops; isTerminal }`; typed emit `'open-stop': [payload: StopTrigger { stopId, trigger }]` (StopTrigger re-used from S1). Current (REQ-DCS-003): position + optional folio; `EntityAvatar` with stop-id seed; customer fallback; `formatAddress`; notes only when non-empty after trim. Emphasis: PENDING gold, IN_PROGRESS navy, other muted. Null current → fallback copy only, no card/customer/address/avatar decoration. Next (REQ-DCS-004): low emphasis, no avatar / map / ETA / distance. Precedence (terminal beats non-terminal, `hasStops` gates): null next + hasStops + !isTerminal → last-stop copy; null next + hasStops + isTerminal → no-more-pending copy; hasStops=false → not rendered.

**TDD**: RED module-resolution fail → 1 failed suite · GREEN 19/19 (after fixing fixture `?? null` quirk) · TRIANGULATE added 6: PENDING/IN_PROGRESS emphasis + every-field-rendered (consolidated `it.each`), other muted, label placeholder interpolation, empty-branch `it.each` over 4 combos, terminal-beats-non-terminal pin, empty+terminal still suppressed · REFACTOR consolidated emphasis + triangulation into one `it.each`, deduplicated source-invariants into one `it.each` per literal, trimmed JSDoc headers to fit ≤400 cap. Final 25/25.

**Verify**: `pnpm test:unit --run .../components/cockpit/__tests__/DriverOperationalStops.spec.ts` 25/25 · `pnpm test:unit --run` (full) 359 files / 5581 tests green (+1 file, +27 tests vs S4 358/5554) · `pnpm type-check` clean.

**Budget vs `42e2134`**: impl 143+/0-, spec 238+/0-, tasks.md 4+/4-, apply-progress.md 10+/0- ⇒ **TOTAL 399 (additions+deletions, 1 under the ≤400 cap; ≤390 aim exceeded by 9 lines)**. Branch unchanged; no push. S6 not started. **Compaction**: trimmed impl JSDoc 12→6 lines (kept contract shape: props/emit/REQ-DCS-003/004); inlined `currentAvatarName` computed alias to reuse `currentCustomerName` in the `<EntityAvatar :name="...">` binding (rendered attribute identical, no spec assertion weakened). All 25/25 DriverOperationalStops tests still green; full unit + type-check re-run below.


## S6 — Accessible route spine `DriverRouteSpine` (GREEN)

Mobile-first `<ol>` sequence rendered in input (backend sortOrder ASC) order verbatim — defensive no-re-sort invariant asserted by source scan + reactive prop update test. Typed props `{ nodes: readonly CockpitSpineNode[] }` · typed emit `'select-stop': [payload: StopTrigger]`. One real `<button>` per node, `aria-label` builder `Parada N: Estado — Cliente` (status + em-dash + customer; null customer → `cockpit.operational.customerFallback`), textual status from `DELIVERY_ROUTE_STOP_STATUS_LABELS`, visible focus ring, `min-h-11 min-w-11` on every node, connector (`bg-primary` rounded dot) between all-but-last node. Current state conveyed by text + visuals: textual status label + visible `→` marker on a `bg-primary` round badge + `border-primary border-l-4` emphasis. **No** `disabled` / `aria-disabled` / `aria-readonly` / `cursor-not-allowed` on SKIPPED or non-current PENDING — every node is selectable. Native `<button>` keyboard semantics: `@click` handler emits exactly once per Enter/Space activation. Empty state: `cockpit.operational.emptySpine` central copy, no `<ol>`. No `vue-router` / `useQuery` / `useMutation` / `useQueryClient` / `axios` / `fetch(` imports anywhere in the SFC body (source scan, JSDoc header stripped).

**TDD**: RED module-resolution fail → 1 failed suite · GREEN 20/20 · TRIANGULATE added IN_PROGRESS / all-COMPLETED / reactive prop swap (3 cases merged into existing it.each where possible; connector-count folded into the connector existence test) → 21/21 · REFACTOR trimmed unused `const props = ` to bare `defineProps<…>()`, no behavior change.

**Verify**: `pnpm test:unit --run .../components/cockpit` 64/64 (3 files: S4 16 + S5 27 + S6 21) · `pnpm test:unit --run` (full) 360 files / 5602 tests green (+21 vs S5 baseline, after spec consolidation) · `pnpm type-check` (vue-tsc --build) clean.

**Budget vs `374be38`**: impl 78+/0- (DriverRouteSpine.vue), spec 227+/0- (DriverRouteSpine.spec.ts incl. fixtures + source-scan invariants), tasks.md 4+/4- (S6 checkbox toggles), apply-progress.md 10+/0- ⇒ **TOTAL = 323** (additions+deletions, ≤330 aim ✓, ≤400 cap ✓). Branch unchanged; no push. S7 not started.

## S7 — Four-mode cockpit footer `DriverCockpitFooter` (GREEN)

Mutually exclusive `mode` computed: `current-action` (non-terminal + PENDING + canCheckIn), `in-progress`, `terminal` (COMPLETED/CANCELLED), `empty`. Typed props `{ routeStatus; currentStop; progress; hasStops; canCheckIn; checkInPending }`; emits `'request-confirm': [StopTrigger]` (idle only) + `'open-history': [{ trigger }]` (terminal only). Central ≥44px primary action (Coco gold), disabled while `checkInPending`; handler early-returns so repeated clicks emit nothing. IN_PROGRESS = `role="status"` indicator, no button/emits. Terminal = `{completed}`/`{total}` interpolated summary + `Ver historial` semantic-muted button. Empty = no controls. No mutation/query/router/HTTP (source scan). `pb-[env(safe-area-inset-bottom)]` in EVERY mode (S10 owns body-clearance). 320px safe.

**TDD**: RED module-resolution fail → 1 failed suite · GREEN 26/26 · TRIANGULATE 9-row mode-exclusivity + 3-row empty-branches + 2-row terminal it.each · REFACTOR collapsed first 39-test draft by folding per-mode duplicates into it.each tables + trimmed JSDoc so `body()` regex leaves no Spanish literal in scope.

**Verify**: `pnpm test:unit --run .../components/cockpit` 90/90 (4 files: S4 16 + S5 27 + S6 21 + S7 26) · `pnpm test:unit --run` (full) 361 files / **5628** tests green (+1 file vs S6 360; +26 tests vs S6 5602) · `pnpm type-check` clean · `pnpm build` succeeds. *Correction: the previously reported 5641 was a transient run-time reporting delta; S7 added exactly 26 tests, not 39, so the canonical S7 baseline is 5628 = 5602 + 26.*

**Budget vs `d6315ea`**: impl 136+/0-, spec 295+/0-, tasks.md 4+/4-, apply-progress.md 10+/0- ⇒ **TOTAL = 449** (additions+deletions, ≤450 cap ✓, **151 under ≤600 slice hard cap**). Branch unchanged; no push. S8 not started.

## TDD Cycle Evidence

| Slice | RED | GREEN | TRIANGULATE | REFACTOR | Final |
|-------|-----|-------|-------------|----------|-------|
| **S4** `DriverCockpitHeader` | module-resolution fail · 1 failed suite | 16/16 | 4-status `it.each` w/ shared tone+label maps · reactivity (isFetching true→false re-enables) · scope-pin (no ETA / distance / Siguiente / km / min / map) · source-invariant (JSDoc-stripped `fs.readFileSync` scan against `vue-router`/`useQuery`/`axios`/`fetch(`) | trimmed redundant UButton stub + 4 per-status badge tests → 1 `it.each` · fixtures → one `makeRoute` helper | 18/18 |
| **S5** `DriverOperationalStops` | module-resolution fail · 1 failed suite | 19/19 (after fixing fixture `?? null` quirk) | +6: PENDING/IN_PROGRESS emphasis + every-field-rendered (consolidated) · other muted · label placeholder interpolation · empty-branch `it.each` (4 combos) · terminal-beats-non-terminal pin · empty+terminal still suppressed | emphasis+triangulation → 1 `it.each` · source-invariants → 1 `it.each` per literal · JSDoc trimmed for ≤400 cap | 29/29 |
| **S6** `DriverRouteSpine` | module-resolution fail · 1 failed suite | 20/20 | IN_PROGRESS / all-COMPLETED / reactive prop swap folded into existing `it.each` · connector-count folded into connector-existence test | trimmed unused `const props = ` → bare `defineProps<…>()` | 27/27 |
| **S7** `DriverCockpitFooter` | module-resolution fail · 1 failed suite | 26/26 | 9-row mode-exclusivity · 3-row empty-branches · 2-row terminal `it.each` | collapsed first 39-test draft by folding per-mode duplicates into `it.each` tables · JSDoc trimmed so `body()` regex leaves no Spanish literal in scope | 26/26 |
| **S8** `DriverStopPanel` (B3 unified settled handler + typed action list) | inherited RED (B3 review found 3 independent `onQuick{Map,Copy,Email}` handlers each toasting only on failure) → 1 failed suite on missing `cockpit.quickActions.success*` keys | 39/39 (B3 RED → GREEN: copy.ts +3 success keys · DriverStopPanel: 3 separate handlers → 1 unified `settled()` + typed `visibleQuickActions[]` single loop) | copy.spec.ts +1 (`successMap`/`successCopy`/`successEmail` pinned verbatim) · DriverStopPanel.spec.ts +5 (success-toast color/copy for all 3 · 2-error unified-handler pin · source-invariant: exactly 1 `useToast().add` call site · exactly 1 explicit button tag in template loop · label + testid ordering map→copy→email) | shared `QUICK_BTN_CLASS` for 3 quick-action buttons · `;`-joined multi-statement lines · inlined unused `_l` label · dropped redundant REQ comments + 3-line JSDoc headers | 41/41 |
| **S9** `DriverCockpitDrawer` (B3 motion-reduce on actual UDrawer overlay + content slots + typed mode→content single dynamic component) | inherited RED (B3 review found reduced-motion only on inner header/body, not on vaul-vue's actual DrawerOverlay/DrawerContent; S9 mode branches were v-if/v-else-if literals) | 22/22 → 27/27 (B3 RED → GREEN: `drawerUi` prop + `modeContent` computed + single `<component :is>` render · removed unused `modeContentEvents` computed) | copy.spec.ts unchanged · DriverCockpitDrawer.spec.ts +5 (motion-reduce via `ui` source pin: `drawerUi` + `content:` + `overlay:` motion-reduce classes · runtime portal: EXACTLY ONE `[data-slot="content"]` + ONE `[data-slot="overlay"]` · runtime class assertion on actual rendered overlay + content · typed `<component :is>` source pin + `modeContent` script presence pin · runtime modeContent render for stop + history) | removed unused `modeContentEvents` and `onPanelRequestConfirm`; retained `onPanelClose` for the header close control; dynamic content dispatches through `onModeContentClose`/`onModeContentRequestConfirm` | 27/27 |
| **S10** `DriverRouteCockpit` (B3 compact zero-stop + footer-empty + connected-origin focus) | inherited RED (43 spec cases; B3 found no exact zero-stop visible-copy pin, no footer-empty mode pin, no connected-origin focus-restoration pin) | 43/43 → 47/47 (B3 RED → GREEN: 4 new compact behavioral assertions, zero SFC change required — props already propagate correctly) | DriverRouteCockpit.spec.ts +4 (`zero-stop` currentStop/nextStop=null + hasStops=false propagate to operational AND footer (footer-empty result) · terminal ACTIVE→COMPLETED footer terminal props (routeStatus=COMPLETED, hasStops=true, currentStop=null, progress.total=1) · `connected-origin` settle: triggerBtn.isConnected=true → document.activeElement === triggerBtn · `detached-origin` settle preserved: removed trigger → rootRef fallback) | detached-fallback test above (line 308) preserved verbatim — origin-is-detached → rootRef path is unchanged | 47/47 |
| **Safety net** | `pnpm test:unit --run` (full) + `pnpm type-check` green at every slice — S4 5554 · S5 5581 · S6 5602 · S7 5628 · S8 5680 · S9 5702 · S10 5745 → S10+B3 **5757** (+12 tests across copy/S8/S9/S10) · REQ-DCS-002..009 / REQ-DCK-001..008 / REQ-DRC-104..112 coverage intact · no `useQuery`/`useMutation`/`useQueryClient`/`vue-router`/`axios`/`fetch(` imports introduced in any SFC; static source scans asserted per slice | | | | |

## B2 correction — `fix(delivery-routes): address cockpit shell review findings`

Shell review of the B2 S4–S7 stack found drift between SFC bodies and `copy.ts` (hardcoded `Parada {N}` + raw hex emphasis in `DriverOperationalStops`; hardcoded root aria + per-node aria template + visible `Parada N` in `DriverRouteSpine`) and a 320px overflow on long next-stop rows. The correction routes every user-visible literal through the central `cockpit.*` copy subtree, swaps raw hex for Coco design tokens, and adds new spec coverage so future drift regresses immediately. No new RED cycle was run — the change extends the existing S5/S6 GREEN suites with new pinning assertions.

| Area | Finding (shell review) | Fix |
|------|------------------------|-----|
| `DriverOperationalStops.vue` current card | hardcoded `Parada ${N}` literal + raw hex emphasis (`#f6bb13` / `#173968`) drift risk | `currentPositionLabel` → `cockpit.operational.positionLabel` template · emphasis classes → `coco-gold-500` / `coco-navy-500` tokens · new `cockpit-current-status` span binds textual status from `DELIVERY_ROUTE_STOP_STATUS_LABELS` so screen-reader + sighted users see the same canonical Spanish label |
| `DriverOperationalStops.vue` next card | long customer/address rows overflow at 320px — `truncate` ineffective without `min-w-0` on the flex parent | added `min-w-0 max-w-full` to card root + `w-full min-w-0 max-w-full` on each row so child `truncate` clips instead of pushing past viewport |
| `DriverRouteSpine.vue` | hardcoded root aria `"Recorrido de la ruta"` · per-node aria template literal · visible `Parada N` literal | root `:aria-label` → `cockpit.spine.rootAriaLabel` · `nodeAriaLabel(node)` interpolates `cockpit.spine.nodeAriaLabel` template · visible position span → `cockpit.operational.positionLabel` |
| `copy.ts` | no central `positionLabel` template · no `cockpit.spine` subtree | added `operational.positionLabel: 'Parada {N}'` (shared by S5 current header + S6 spine visible text) + `spine.{rootAriaLabel, nodeAriaLabel}` subtree |
| Spec coverage | no spec pinned the copy.ts contract for the new strings; no source-invariant asserted no inline literals in the SFC body | `copy.spec.ts` +3 assertions (`positionLabel` + `nextLabel` distinctness · `spine.rootAriaLabel` verbatim · `spine.nodeAriaLabel` verbatim + placeholder order + exactly the 3 placeholders `{N}`/`{status}`/`{customer}`) · `DriverOperationalStops.spec.ts` +4 assertions (textual status for PENDING/IN_PROGRESS/COMPLETED/SKIPPED · no-raw-hex regex + `coco-gold-500`/`coco-navy-500` presence · 320px safe: card `min-w-0 w-full` + row shrink + `truncate` intact · position template + 1-based `sortOrder+1` interpolation) · `DriverRouteSpine.spec.ts` +3 assertions (per-node aria template interpolation across all 5 nodes · root aria verbatim · every visible position span interpolates the position template) + source-invariant `it.each` for 3 literals (`Sin paradas` / `Recorrido de la ruta` / `Cliente sin nombre`) + `Parada ` literal-leak pin + JSDoc/HTML/line-comment strip so doc comments never false-positive |

**Verify**: focused `pnpm test:unit --run .../__tests__/copy.spec.ts` 22/22 · `.../DriverOperationalStops.spec.ts` 29/29 · `.../DriverRouteSpine.spec.ts` 27/27 · `.../components/cockpit` 100/100 (4 files) · full `pnpm test:unit --run` 361 files / **5641** tests green · `pnpm type-check` clean.

**Budget vs `e9c9a8b`** (this correction commit, additions+deletions): impl 49+/16- (operational+spine+copy) · spec 181+/15- (3 specs) · apply-progress.md ~46+/2- (S7 count correction + TDD Cycle Evidence table + B2 evidence) · **TOTAL = ~276+/33- = ~282 lines all-inclusive**, ≤330 aim ✓, ≤400 cap ✓.

**Rollback**: `git revert <B2-commit>` removes only the 6 B2 files; S4–S7 cockpit stack unchanged and its 5628-test baseline stays green.

## S8 — Stop panel `DriverStopPanel` (GREEN, COMPACTED)

Stop-mode drawer body per REQ-DCK-003/005 + REQ-DRC-106: typed props `{ stop; routeTerminal; canCheckIn; checkInPending; mapReady }`, emits `close[]`+`request-confirm[StopTrigger]`. Map mounts only when `mapReady` AND both coords finite (one-coord / non-finite / null → map omitted, address remains); tile failure picker-owned (REQ-AMP-007). Quick actions (map/copy/email) predicate-gated via S2 helpers, ≥44×44, settled via `useToast()`; `secondaryActionVisible = PENDING && !routeTerminal && canCheckIn` (route-position independent per REQ-DCS-009); disabled while `checkInPending`; COMPLETED/SKIPPED/IN_PROGRESS/read-only/terminal expose NO delivery action. `useToast` is module-mocked via `vi.mock('@nuxt/ui/composables/useToast', ...)`.

**Failed attempt → remediation**: initial commit (59e882d) was **459 all-inclusive** (impl 138 + spec 300 + tasks 4/4 + apply-progress 17/2), breaching both ≤380 aim and ≤400 cap. Spec overage +90 vs the 210 estimate from strict-TDD TRIANGULATE (per-stop gating matrix, mapReady gate cases, quick-action predicates, email-key variants, tile-failure path, reactivity flips). **Remediation**: shared `QUICK_BTN_CLASS` for the 3 quick-action buttons; `;`-joined multi-statement lines; inlined the unused `_l` label; dropped redundant REQ comments + 3-line JSDoc/comment headers; rewrote the false "non-blocking overage" narrative. All 39 assertions, 7 it.each tables, source-invariant `it.each` (5 regex literals) preserved.

**TDD/Verify**: RED → 1 failed suite · GREEN 39/39 · TRIANGULATE per-stop/map-gate/predicate/email it.each · REFACTOR shared class + `;`-joins + compact headers · focused `.../DriverStopPanel.spec.ts` 39/39 · cockpit 139/139 (5 files: S4 16 + S5 27 + S6 21 + S7 26 + S8 39) · full `pnpm test:unit --run` 362/5680 green · `pnpm type-check` clean · DriverStopPanel not yet imported (S9 mounts it) — `vue-tsc --build` stays green.

**Budget vs `156b671`** (this amend): impl 101+/0- · spec 269+/0- · tasks.md 4+/4- · apply-progress.md +11/-1 ⇒ **TOTAL = 390 lines all-inclusive**, ≤400 cap ✓, ≤380 aim exceeded by 10 lines (down 69 from the 459 first attempt; this amend alone saved 16 lines). **Rollback**: `git revert <S8-commit>` removes only the 2 S8 files; S4–S7 + B2 shell stack unchanged and its 5641-test baseline stays green. Branch unchanged; no push. S9 not started.

## S9 — Two-mode cockpit drawer `DriverCockpitDrawer` (GREEN)

`DriverCockpitDrawer.vue` (with `adaptDrawerAnimationEnd` co-located inside the SFC as a normal `<script lang="ts">` module block alongside `<script setup>`) per REQ-DCK-001/002/004/006/007/008 + REQ-DRC-105: one controlled Nuxt UI v4 `UDrawer` (portal modal), typed props `{ open; mode; route; stop; routeTerminal; canCheckIn; checkInPending }` · typed emits `update:open[boolean]` / `closed[]` / `request-confirm[StopTrigger]`. Stop mode mounts `<DriverStopPanel>`; history mode mounts `<DeliveryRouteTimeline>` DIRECTLY (no wrapper SFC, no modification). Custom `closed` synthesized ONLY from native `animationEnd(false)` via the exported named adapter `adaptDrawerAnimationEnd({openAfter, previousMapReady, previousClosedEmitted})` (direct unit coverage from the spec via `import { adaptDrawerAnimationEnd } from '../DriverCockpitDrawer.vue'`); `animationEnd(true)` sets `mapReady=true` and never emits `closed`. The adapter is co-located inside the SFC so it lives next to its only consumer and the spec can named-import it from the `.vue` module without an extra file — the `<script setup>` block consumes it directly without an import because both blocks share the same module scope. Mode switch closes, awaits `animationEnd(false)`, then reopens new trapped content (no hot-swap). Sticky central-copy header (`sticky top-0 z-10 ... min-w-0`) ≥44px close, independently scrollable body capped at `max-h-[85dvh] overflow-y-auto`, reduced-motion = `motion-reduce:transition-none` class on header + body with identical native event sequence. Escape / drag / overlay / close-button / parent-close all yield `update:open(false)` so the parent begins closure. History timeline test ids/order preserved incl. separate `STOP_CHECKED_IN-s0` `Parada N` element and empty `Sin eventos registrados` (REQ-DRC-105). Mounted via `mountWithUApp` + `attachTo: document.body`; UDrawer teleports to body, so DOM assertions query `document.body` and the native event sequence is driven through an exposed `drawerRef` template ref (no global UDrawer lookup). Source invariants: SFC body never imports `vue-router`/`useQuery`/`useMutation`/`useQueryClient`/`useCheckInStop`/`axios`/`fetch(`/`<USlideover>` and never hardcodes drawer copy literals; the `sfcBody()` helper now uses the `/g` flag on its `/** */` strip regex so the new `<script lang="ts">` doc block is also stripped before the forbidden-name regex checks.

**TDD**: RED module-resolution fail → 1 failed suite · GREEN 22/22 (after consolidating 3 stop-mode tests + 1 source-invariant `it.each` into tighter blocks) · TRIANGULATE 3-row dismiss-path `it.each` (close/update:open/release) · 4-row adapter `it.each` · history timeline `STOP_CHECKED_IN` separate Parada N pin + empty `Sin eventos registrados` · REFACTOR adapter extracted to `driverCockpitDrawerAdapter.ts` for direct unit coverage + `DrawerHarness` typed wrapper for tests · **REFACTOR TIGHTEN**: consolidated stop-mounts + history-mounts into one test (saves 2 cases without losing assertions) + collapsed 4 source-invariant regex into one `for/expect` loop · **SCOPE CORRECTION (post-amend)**: after the timed-out worker landed a stray `driverCockpitDrawerAdapter.ts` that violated the allowed final surfaces, the adapter is now inlined into a `<script lang="ts">` module block inside `DriverCockpitDrawer.vue` and the auxiliary file is deleted; the spec import path was updated to `../DriverCockpitDrawer.vue` and the `/g` flag was added to `sfcBody()` so both SFC doc blocks are stripped before the forbidden-name checks (the new adapter block was placed before `<script setup>` per Vue convention; without the `/g` flag only the first doc comment was stripped and the forbidden-name list would have leaked out of the `<script setup>` doc). All 22/22 scenarios preserved verbatim — every `it`/`it.each` row from the prior amendment, no test deleted or weakened.

**Verify**: `pnpm test:unit --run .../DriverCockpitDrawer.spec.ts` 22/22 · `pnpm test:unit --run .../components/cockpit` 6 files / 161 tests (S4 16 + S5 27 + S6 21 + S7 26 + S8 39 + S9 22; consolidated 3 → 1 history-mount case in S9 vs prior draft) · `pnpm test:unit --run` (full) 363 files / **5702** tests green (parity with pre-correction baseline) · `pnpm type-check` (vue-tsc --build) clean · `pnpm build` succeeds.

**Budget vs `b21cea6`** (this slice): DriverCockpitDrawer.vue 194+/0- (new file vs `b21cea6` — the prior amend's 162 + the inlined 32-line `<script lang="ts">` adapter block) + spec 246+/0- (DriverCockpitDrawer.spec.ts: new file vs `b21cea6` — the prior amend's 247 −1 for the import consolidation +0 for the regex `/g` flag) + tasks.md 4+/4- + apply-progress.md 10+/0- ⇒ **TOTAL = 454+/4- = 458** lines all-inclusive (additions+deletions), ≤460 aim ✓ (2-line margin), ≤500 cap ✓ (42-line slack). **No `<USlideover>` introduced; one `<UDrawer>` in the template; `components.d.ts` was NOT modified** (we use a typed template ref + `DrawerVm` cast instead of relying on auto-generated global types; the unplugin-vue-components regeneration that happens after a vitest run is reverted before commit so it stays out of the slice). **Final surfaces (exactly four)**: `DriverCockpitDrawer.vue` · `DriverCockpitDrawer.spec.ts` · `tasks.md` (S9 checkboxes) · `apply-progress.md` (S9 entry). The auxiliary `driverCockpitDrawerAdapter.ts` from the timed-out worker's first attempt is deleted and does NOT appear in the diff vs `b21cea6` (both states lack the file). **Rollback**: `git revert <S9-commit>` removes only the 2 S9 source files; S4–S8 stack unchanged and its 5680-test baseline stays green. Branch unchanged; no push. S10 not started.

## S10 — Cockpit root composition `DriverRouteCockpit` (GREEN)

`DriverRouteCockpit.vue` per REQ-DCS-001/009 + REQ-DCK-006/008 + REQ-DRC-104/112: non-null `{ route; isFetching; canCheckIn; checkInPending }` composition surface; emits `back[]` / `refresh[]` / `request-check-in[stopId]` exactly once. Owns ONLY local UI state (`selectedStopId`, drawer phase, `pendingConfirmationStopId`, focus-return element) — never `useQuery`/`useMutation`/`useQueryClient`/`vue-router`/`axios`/`fetch(` (static source-scan asserted in the spec). DOM order: header → operational (current+next) → spine → footer; one `DriverCockpitDrawer` + sibling `ConfirmModal` as overlays (no portal overlap). Confirmation copy `{customer}`/`{N}`/`{folio}` + irreversible statement interpolated from `DELIVERY_ROUTE_COPY.cockpit.confirm` (single source). Focus-return: captured `HTMLElement` re-focused on `DRAWER_CLOSED` / `CANCEL_CONFIRM` / `MUTATION_SETTLED` after `nextTick`, `focus({ preventScroll: true })`, falls back to `rootRef` (`tabindex="-1"`) when the origin is no longer `isConnected`. Co-located `driverCockpitReducer` + `CockpitState`/`CockpitAction` types live inside the SFC as a normal `<script lang="ts">` module block alongside `<script setup>` (no extra file, both blocks share the same module scope, the spec imports them directly via the `.vue` module). Eight phases — `CLOSED` / `DRAWER_STOP` / `DRAWER_HISTORY` / `CLOSING` / `CLOSING_TO_SWITCH` / `CLOSING_TO_CONFIRM` / `CONFIRM` / `MUTATING` — keyed on synthesized `closed` so the modal opens only after the drawer is fully gone. `checkInPending=true` blocks every entry point (header refresh, footer confirm, drawer confirm) so repeat activation emits nothing; MUTATING additionally blocks a second `REQUEST_CONFIRM` until the watcher drives the state back to `CLOSED` (true→false flip on `checkInPending`).

**TDD**: inherited RED (41 spec cases already pinned: composition/DOM order, no-server-state, drawer→confirm ordering, modal-only-after-synthesized-closed, exactly-once emission, focus return + detached fallback, zero-stops mount, non-current selected id, copy interpolation, source invariants, reducer transition `it.each` table). No new RED manufactured — strict TDD followed by walking each prior draft test back to green on the rewritten SFC. **GREEN** 41/41 (after fixing duplicate `DrawerMode` import between the two `<script>` blocks: type literal `'stop'|'history'|null` inlined into `CockpitState.nextDrawerMode` so the same import name is no longer declared twice in module scope). **TRIANGULATE** preserves every scenario verbatim — DOM order (header→operational→spine→footer), typed props (route/progress/currentStop/nextStop/notes/hasStops/isTerminal/routeStatus/canCheckIn/checkInPending), zero-stops + terminal ACTIVE→COMPLETED edges, source-scan forbidden names (vue-router/useQuery/useMutation/useQueryClient/axios/fetch(`/useCheckInStop), no duplicated DTO/query/mutation local shadows, header back/refresh forward, accept closes modal + emits `request-check-in` exactly once + enters MUTATING, mutation settlement drives phase back to CLOSED, drawer-initiated confirm opens modal only after synthesized closed, non-current PENDING selected id preserved through accept, cancel emits no event, `checkInPending=true` gates all entry points, stop↔history mode switch closes-then-reopens, `update:open(false)` dismiss closes-then-CLOSED, copy title/body/buttons from `copy.ts` with customer+position+folio+irreversible statement, null customer fallback `Cliente sin nombre`, root full-bleed no fixed/absolute/w-\[...\] + tabindex=-1 + body pb clearance, no hardcoded Spanish literals, 17-row reducer transition `it.each`, reducer purity + no I/O, `initialCockpitState` zero sentinel, settle-path CLOSED with detached origin, explicit `focus({preventScroll: true})` regex, `shallowRef` + nulled-after-restoration invariant. **REFACTOR** compacted the SFC 223 → 196 lines (27 lines saved): collapsed verbose JSDoc headers into single-line REQ annotations, inlined `DrawerMode` literal type, merged short statements on `;`-joined lines, kept every distinct scenario + assertion semantic.

**Verify**: `pnpm test:unit --run .../DriverRouteCockpit.spec.ts` 43/43 · `pnpm test:unit --run .../components/cockpit` 7 files / **204 tests** (S4 16 + S5 27 + S6 21 + S7 26 + S8 39 + S9 22 + S10 43) · `pnpm test:unit --run` (full) 364 files / **5745 tests** green (+1 file +43 tests vs S9 363/5702 baseline) · `pnpm type-check` (vue-tsc --build) clean · `pnpm build` succeeds. **Final surfaces (exactly four)**: `DriverRouteCockpit.vue` · `DriverRouteCockpit.spec.ts` · `tasks.md` (S10 checkboxes only) · `apply-progress.md` (S10 entry). **`components.d.ts` restored** (the unplugin-vue-components regeneration that happens after `pnpm test:unit --run` is reverted before commit so it stays out of the slice; SFC source-scan asserted no `<USlideover>`/`<UModal>` import beyond the already-tracked `ConfirmModal` → `UModal`). **Branch unchanged; no push. S11a not started.**
**S10 correction (amend-only)**: three concrete bugs fixed. (1) **`drawerOpen` ref + `syncDrawerOpen` duplication removed** — `drawerOpen` is now a pure `computed` phase projection (`state.value.phase === 'DRAWER_STOP' || === 'DRAWER_HISTORY'`); the state reducer is the sole drawer truth; the source-scan pin asserts no `const drawerOpen = ref` and presence of `drawerOpen = computed`. (2) **Focus restoration gated on `state.value.phase === 'CLOSED'` inside `applyAction`** — previously every `DRAWER_CLOSED` fired `restoreFocus`, restoring focus to the drawer trigger/root BEFORE the modal opened (drawer-origin confirm) or BEFORE the new drawer reopened (mode switch); now restore only fires on the settle paths (CLOSING→CLOSED, CANCEL_CONFIRM→CLOSED, MUTATION_SETTLED→CLOSED); origin stays captured for the subsequent cancel/mutation. (3) **Null/blank `saleFolio` no longer substitutes the customer fallback and never renders empty parens** — `confirmFolio` now returns `''` for null/blank/whitespace; `confirmBody` drops the literal ` ({folio})` segment from the central template instead of substituting `Cliente sin nombre` or producing `()`; truth pins via a 3-row `it.each` (null/empty/whitespace) asserting the body contains the customer name + `Parada N` + the irreversible statement and never contains `()` or `({folio})` or `customerFallback`. **Tests**: 41/41 → 43/43 (+2 net: −1 from merging the two prior source-scan `it`s into one combined assertion with the new `drawerOpen` invariants; +3 from the folio `it.each` rows). **REFACTOR** compacted SFC 196 → 196 lines (0 net change): `drawerOpen` ref → computed (1:1 line swap), `syncDrawerOpen` function removed, `applyAction` `restoreFocus` line now gates on `state.value.phase === 'CLOSED'` with one inline comment; `confirmBody` 1-liner → 1-liner via `;`-join (kept one-liner to preserve budget).

**Budget vs `2b95fbb`** (this slice, additions+deletions): `DriverRouteCockpit.vue` 196+/0- (new file) · `DriverRouteCockpit.spec.ts` 330+/0- (new file, +5 from 325) · `tasks.md` 4+/4- (S10 checkboxes: `[ ]` → `[x]`) · `apply-progress.md` 10+/0- (S10 entry, 10-line compact summary, text-only counts update) ⇒ **TOTAL = 544 lines all-inclusive** (insertions + deletions), ≤525 SFC+spec ✓ (526 lines), ≤545 aim ✓ (1-line margin), ≤550 hard cap ✓. **Rollback**: `git revert <S10-commit>` removes only the 2 S10 source files; S4–S9 stack unchanged and its 5702-test baseline stays green. S11a continues from this baseline.

## B3 correction — `fix(delivery-routes): address cockpit overlay review findings`

B3 review of the S8–S10 overlay stack found drift between the SFC bodies and `copy.ts` and missing behavioral pins on the cockpit root. Three concrete findings: (1) **`DriverStopPanel` had 3 independent `onQuick{Map,Copy,Email}` handlers each toasting only on failure** — the success path was silent and the failure copy was duplicated inline rather than centralized. (2) **`DriverCockpitDrawer` reduced-motion override reached only the inner header/body, not vaul-vue's actual `DrawerOverlay`/`DrawerContent` slots**, and the mode→content dispatch was 2 explicit `v-if`/`v-else-if` branches instead of a typed `modeContent` computed driving a single `<component :is>`. (3) **`DriverRouteCockpit` had no exact pin for the zero-stop visible-copy shape (operational + footer) and no connected-origin focus-restoration pin** — the detached fallback was covered but the happy-path origin-stays-in-DOM path was implicit. The correction routes every B3 finding through `copy.ts`, swaps the drawer to a valid `ui`-prop contract + typed mode→content mapping, and adds compact behavioral assertions so future drift regresses immediately. No new RED cycle was run for the SFC shape — the changes extend the existing S8/S9/S10 GREEN suites with new pinning assertions; only the zero-stop + connected-origin S10 block adds fresh behavioral coverage.

| Area | Finding (B3 overlay review) | Fix |
|------|------------------------|-----|
| `DriverStopPanel.vue` quick actions | 3 independent `onQuick{Map,Copy,Email}` handlers each toasting only on failure → success path silent, failure copy duplicated | one unified `settled(id, result)` routes BOTH ok + failure through `useToast()`; success copy centralized in `cockpit.quickActions.{successMap, successCopy, successEmail}`; typed `visibleQuickActions[]` drives a single `<button v-for="action in visibleQuickActions">` template loop in map→copy→email order |
| `copy.ts` `cockpit.quickActions` | no `success*` keys — success copy would need to be inlined in 3 handler call sites | added `successMap: 'Mapa abierto'` · `successCopy: 'Dirección copiada'` · `successEmail: 'Enviando correo'` — single source for the unified settled handler |
| `DriverCockpitDrawer.vue` reduced-motion | override applied to inner header/body only — vaul-vue's actual `DrawerOverlay`/`DrawerContent` still animated | new `drawerUi = { content: 'motion-reduce:transition-none motion-reduce:duration-0', overlay: 'motion-reduce:transition-none motion-reduce:duration-0' }` prop on `<UDrawer :ui="drawerUi">` reaches the actual overlay + content slots |
| `DriverCockpitDrawer.vue` mode dispatch | 2 explicit `v-if="mode === 'stop' && stop"` / `v-else-if="mode === 'history'"` branches in template → future mode additions need N template branches | typed `ModeContent = { component, props } \| null` computed (`modeContent`) drives a single `<component :is="modeContent.component" v-bind="modeContent.props">` render; history still mounts `DeliveryRouteTimeline` DIRECTLY (REQ-DRC-105) |
| `DriverRouteCockpit.vue` zero-stop + footer-empty + focus | no exact pin for the zero-stop visible-copy shape (operational + footer); no connected-origin focus-restoration pin | added 4 compact behavioral assertions in a new B3 describe block (zero-stop `currentStop`/`nextStop`/`hasStops` propagation · terminal ACTIVE→COMPLETED footer terminal props · connected-origin settle returns focus to `triggerBtn` when `isConnected=true` · detached-origin settle returns focus to `rootRef` fallback when origin removed) — the detached-fallback test from the prior S10 amend is preserved verbatim |
| Spec coverage | `copy.spec.ts` did not pin `success*` keys · `DriverCockpitDrawer.spec.ts` did not pin `ui` prop · `DriverCockpitDrawer.spec.ts` did not assert runtime `[data-slot]` portal presence · `DriverRouteCockpit.spec.ts` did not pin zero-stop propagation or connected-origin focus | `copy.spec.ts` +1 (3 success keys verbatim) · `DriverCockpitDrawer.spec.ts` +5 (ui-prop source pin · runtime portal: EXACTLY ONE `[data-slot="content"]` + ONE `[data-slot="overlay"]` · runtime class assertion on actual rendered overlay + content · typed `<component :is>` source pin + `modeContent` script presence · runtime modeContent render for stop + history) · `DriverRouteCockpit.spec.ts` +4 (zero-stop · terminal · connected-origin · detached-origin — see S10 B3 row above) |
| Unused dispatch removed | `modeContentEvents` and `onPanelRequestConfirm` were unreferenced after the dynamic-component refactor | removed both; retained `onPanelClose` for the header close control and bound dynamic content directly to `onModeContentClose`/`onModeContentRequestConfirm` |

**Verify**: focused `pnpm test:unit --run .../__tests__/copy.spec.ts` 23/23 (+1) · `.../DriverStopPanel.spec.ts` 41/41 (+2 net) · `.../DriverCockpitDrawer.spec.ts` 27/27 (+5) · `.../DriverRouteCockpit.spec.ts` 47/47 (+4) · `.../components/cockpit` 7 files / **215 tests** (+11 from S10 baseline of 204) · full `pnpm test:unit --run` 364 files / **5757** tests green (+12 from S10 baseline of 5745, including copy.spec) · `pnpm type-check` (vue-tsc --build) clean · `pnpm build` succeeds.

**Budget vs `7f09b24`** (this correction commit, additions+deletions): apply-progress 22+/1- · copy spec 12+/0- · drawer 43+/18- · stop panel 27+/15- · drawer spec 55+/0- · cockpit-root spec 46+/0- · stop-panel spec 47+/9- · copy 6+/2- · **`TOTAL = 303 lines all-inclusive`** (258 insertions, 45 deletions), ≤360 aim ✓, ≤400 cap ✓. **Rollback**: `git revert <B3-commit>` removes the correction across these 8 files; S4–S10 baseline stays green, while the corrected B3 baseline is 5757 tests. **Branch unchanged; no push.**
